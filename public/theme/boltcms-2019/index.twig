{# {% extends 'partials/_master.twig' %}

{% block header %}
{% include 'partials/_header.twig' with {'compact': false } %}
{% endblock %}

{% block content %}

{% setcontent blocks = "blocks" orderby "order" limit 100 %}

  {% for block in blocks %}

    {% include [
        'partials/_section_' ~ block.variant|default('default') ~ '.twig',
        'partials/_section_default.twig'
      ] %}

  {% endfor %}

{% endblock content %} #}
