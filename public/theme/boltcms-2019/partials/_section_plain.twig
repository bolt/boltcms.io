<section>
    <div class="container">
        <div class="row align-items-center">
            <div class="col-lg-12 order-lg-1">
                <div class="p-5">
                    <h2>{{ block.title }} {% if user %}<small>{{ block.order }}</small>{% endif %}</h2>
                    {{ block.content }}
                    {#              {{ dump(block.content) }}#}
                </div>
            </div>
        </div>
    </div>
</section>
