<section>
    <div class="container">
        <div class="row align-items-center">
            <div class="col-lg-4 order-lg-2">
                <div class="p-3">
                    <img class="img-fluid {{ block.image.title|default() }}" src="{{ block|image }}" alt="{{ block.image.alt|e }}">
                </div>
            </div>
            <div class="col-lg-8 order-lg-1">
                <div class="p-5">
                    {% include 'partials/_title.twig' %}
                    {{ block.content }}
                </div>
            </div>
        </div>
    </div>
</section>
