<section>
    <div class="container">
        <div class="row align-items-center">
            <div class="col-lg-4 order-lg-2 p-3">
                <img class="img-fluid {{ block.image.title|default() }}" src="{{ block|image }}" alt="{{ block.image.alt|e }}">
            </div>
            <div class="col-lg-8 order-lg-1 p-5">
                {% include 'partials/_title.twig' %}
                {{ block.content }}
            </div>
        </div>
    </div>
</section>
